# Master script — GitHub Actions will call this
# We'll build this out as we add each scraper

def main():
    print("Update script running...")
    # Step 2: scrape FBref
    # Step 3: compute xG table
    # Step 4: fetch odds + compute vegas table

if __name__ == "__main__":
    main()
